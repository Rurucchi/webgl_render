/* Triangle 2D */
const triangle = new Float32Array([
  // vertex
  0.0, +0.75, 1,
  // tex
  1, 1,
  // normal
  0, 0, 0,
  // color
  1, 0.5, 0, 1,

  // vertex
  0.75, -0.5, 0,
  // tex
  1, 1,
  // normal
  0, 0, 0,
  // color
  1, 1, 1, 1,

  // vertex
  -0.75, -0.5, 0,
  // tex
  1, 1,
  // normal
  0, 0, 0,
  // color
  1, 0, 0.5, 1,
]);

export default { triangle };
